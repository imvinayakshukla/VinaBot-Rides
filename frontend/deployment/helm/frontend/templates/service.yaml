apiVersion: v1
kind: Service
metadata:
  name: {{ include "vinbot-rides-frontend.fullname" . }}
  labels:
    {{- include "vinbot-rides-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vinbot-rides-frontend.selectorLabels" . | nindent 4 }}
